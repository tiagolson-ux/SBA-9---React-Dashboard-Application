import AppLayout from "./components/layout/AppLayout";
import Dashboard from "./pages/Dashboard";

// Note to self: App is the root component
export default function App() {
  return (
    <AppLayout>
      <Dashboard />
    </AppLayout>
  );
}
// Note to self: App is the root component